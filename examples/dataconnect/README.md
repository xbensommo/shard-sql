# Data Connect example shape

This folder is intentionally documentation-only.

A real project would usually keep Data Connect generated code in the application repo, not in the package itself.
The package should stay adapter-focused and database-agnostic.
